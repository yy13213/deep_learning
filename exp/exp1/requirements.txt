# 安装方式（CUDA 12.8，RTX 6000 Ada 等 NVIDIA GPU）：
#   pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu128
# 其余依赖：
#   pip install -r requirements.txt
torch>=2.10.0
torchvision>=0.25.0
Pillow>=9.5.0
matplotlib>=3.7.0
numpy>=1.24.0
jupyter>=1.0.0
ipywidgets>=8.0.0
